import { Button, Col, Form, Row } from "react-bootstrap";
import SectionHeader from "./SectionHeader";
export default function Projects({ items, errors, onChange, onAdd, onRemove }) { return <section className="form-section"><SectionHeader number="04" title="Projects" description="Share work that best shows your problem-solving ability." onAdd={onAdd} addLabel="Add project" />{items.map((item, index) => <div className="entry-card" key={item.id}><div className="entry-card-top"><span>Project #{index + 1}</span>{items.length > 1 && <Button type="button" variant="link" className="remove-button" onClick={() => onRemove(item.id)}>Remove</Button>}</div><Row className="g-4"><Col md={6}><Field label="Project title *" name="title" item={item} error={errors[index]?.title} onChange={onChange} placeholder="Portfolio website" /></Col><Col md={6}><Field label="Project link" name="link" type="url" item={item} error={errors[index]?.link} onChange={onChange} placeholder="https://..." /></Col><Col xs={12}><Form.Group><Form.Label>Description *</Form.Label><Form.Control as="textarea" rows={3} value={item.description} placeholder="Briefly explain the problem, your contribution, and outcome." onChange={e => onChange(item.id, "description", e.target.value)} isInvalid={Boolean(errors[index]?.description)} /><Form.Control.Feedback type="invalid">{errors[index]?.description}</Form.Control.Feedback></Form.Group></Col></Row></div>)}</section>; }
function Field({label,name,type="text",item,error,onChange,placeholder}) { return <Form.Group><Form.Label>{label}</Form.Label><Form.Control type={type} value={item[name]} placeholder={placeholder} onChange={e=>onChange(item.id,name,e.target.value)} isInvalid={Boolean(error)}/><Form.Control.Feedback type="invalid">{error}</Form.Control.Feedback></Form.Group>; }
