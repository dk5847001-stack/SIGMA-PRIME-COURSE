import { Button, Col, Form, Row } from "react-bootstrap";
import SectionHeader from "./SectionHeader";
export default function Certifications({ items, errors, onChange, onAdd, onRemove }) { return <section className="form-section"><SectionHeader number="05" title="Certifications" description="Add credentials that strengthen your application." onAdd={onAdd} addLabel="Add certification" />{items.map((item,index)=><div className="entry-card" key={item.id}><div className="entry-card-top"><span>Certification #{index+1}</span>{items.length>1&&<Button type="button" variant="link" className="remove-button" onClick={()=>onRemove(item.id)}>Remove</Button>}</div><Row className="g-4"><Col md={6}><Field label="Certification name *" name="name" item={item} error={errors[index]?.name} onChange={onChange} placeholder="AWS Cloud Practitioner"/></Col><Col md={6}><Field label="Issuing organization *" name="issuer" item={item} error={errors[index]?.issuer} onChange={onChange} placeholder="Amazon Web Services"/></Col><Col md={6}><Field label="Issue date" name="date" type="month" item={item} error={errors[index]?.date} onChange={onChange}/></Col><Col md={6}><Field label="Credential link" name="link" type="url" item={item} error={errors[index]?.link} onChange={onChange} placeholder="https://..."/></Col></Row></div>)}</section>; }
function Field({label,name,type="text",item,error,onChange,placeholder}) { return <Form.Group><Form.Label>{label}</Form.Label><Form.Control type={type} value={item[name]} placeholder={placeholder} onChange={e=>onChange(item.id,name,e.target.value)} isInvalid={Boolean(error)}/><Form.Control.Feedback type="invalid">{error}</Form.Control.Feedback></Form.Group>; }
