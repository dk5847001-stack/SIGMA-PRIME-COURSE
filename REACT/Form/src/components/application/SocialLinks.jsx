import { Col, Form, Row } from "react-bootstrap";
import SectionHeader from "./SectionHeader";
export default function SocialLinks({ data, errors, onChange }) { const fields=[['github','GitHub profile','https://github.com/username'],['linkedin','LinkedIn profile','https://linkedin.com/in/username'],['portfolio','Portfolio website','https://yourportfolio.com']]; return <section className="form-section"><SectionHeader number="07" title="Social links" description="Optional, but a strong way to showcase your work."/><Row className="g-4">{fields.map(([name,label,placeholder])=><Col md={4} key={name}><Form.Group controlId={`social-${name}`}><Form.Label>{label}</Form.Label><Form.Control type="url" name={name} value={data[name]} placeholder={placeholder} onChange={onChange} isInvalid={Boolean(errors[name])}/><Form.Control.Feedback type="invalid">{errors[name]}</Form.Control.Feedback></Form.Group></Col>)}</Row></section>; }
