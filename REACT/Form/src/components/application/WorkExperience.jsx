import { Button, Col, Form, Row } from "react-bootstrap";
import SectionHeader from "./SectionHeader";
export default function WorkExperience({ items, errors, onChange, onAdd, onRemove }) { return <section className="form-section"><SectionHeader number="06" title="Work experience" description="Internships, freelance engagements, and part-time roles all count." onAdd={onAdd} addLabel="Add experience" />{items.map((item,index)=><div className="entry-card" key={item.id}><div className="entry-card-top"><span>Experience #{index+1}</span>{items.length>1&&<Button type="button" variant="link" className="remove-button" onClick={()=>onRemove(item.id)}>Remove</Button>}</div><Row className="g-4"><Col md={6}><Field label="Company *" name="company" item={item} error={errors[index]?.company} onChange={onChange} placeholder="Company name"/></Col><Col md={6}><Field label="Role *" name="role" item={item} error={errors[index]?.role} onChange={onChange} placeholder="Frontend intern"/></Col><Col md={6}><Field label="Start date" name="startDate" type="month" item={item} error={errors[index]?.startDate} onChange={onChange}/></Col><Col md={6}><Field label="End date" name="endDate" type="month" item={item} error={errors[index]?.endDate} onChange={onChange}/></Col><Col xs={12}><Form.Group><Form.Label>Key responsibilities *</Form.Label><Form.Control as="textarea" rows={3} value={item.description} onChange={e=>onChange(item.id,"description",e.target.value)} placeholder="Describe your impact and key responsibilities." isInvalid={Boolean(errors[index]?.description)}/><Form.Control.Feedback type="invalid">{errors[index]?.description}</Form.Control.Feedback></Form.Group></Col></Row></div>)}</section>; }
function Field({label,name,type="text",item,error,onChange,placeholder}) { return <Form.Group><Form.Label>{label}</Form.Label><Form.Control type={type} value={item[name]} placeholder={placeholder} onChange={e=>onChange(item.id,name,e.target.value)} isInvalid={Boolean(error)}/><Form.Control.Feedback type="invalid">{error}</Form.Control.Feedback></Form.Group>; }
